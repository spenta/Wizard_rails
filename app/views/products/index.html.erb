<%= link_to "Accueil", :root%>
<p><%= Nombre de produits :  #{Product.all.size}%></p>
<table>
  <% Product.all.each do |p| -%>
  <tr>
    <td><%= link_to p do%>
    <%= image_tag p.small_img_url,{:height => 60} %>
  <% end %></td>
    <td><%=  p.brand.name%> </td>
    <td><%=  p.name%></td>
    </td>
  </tr>
  <% end -%>
</table>
