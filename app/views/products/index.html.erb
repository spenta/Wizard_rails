<%= link_to "Accueil", :root%>
<table>
  <% Product.all.each do |p| -%>
  <tr>
    <td><%= image_tag p.small_img_url, {:height => 60}%></td>
    <td><%=  p.brand.name%> </td>
    <td><%=  p.name%></td>
    </td>
  </tr>
  <% end -%>
</table>
