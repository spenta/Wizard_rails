<%= render :partial => 'vspacer', :locals => {:is_first_column => is_first_column} %>
<td class="bestpickcolumn" colspan="2" >
  <%= link_to product_path(p.product_id) do %>
    <% product = Product.find(p.product_id) -%>
    <%= image_tag product.big_img_url, :alt => t(:bestpick_img)+" "+product.infos[:brand_name]+" "+product.name, :class => "featuredimg"%>
  <% end -%>
</td>
