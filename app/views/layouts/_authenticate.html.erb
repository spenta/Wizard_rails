<% if current_user %>
  Connecté en tant que <%= current_user.name %>. 
  <%= link_to "Déconnexion", logout_url.gsub(/^http:/, "https:") %>
<% else %>
  <%= link_to "Connexion", login_url.gsub(/^http:/, "https:")%>
<% end %>
